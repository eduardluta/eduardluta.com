{
  "name": "eduardluta-cms",
  "version": "1.0.0",
  "description": "Eduard Luta personal website with Decap CMS",
  "private": true,
  "scripts": {
    "build": "node build-posts.js",
    "watch": "nodemon --watch _posts -e md --exec 'npm run build'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "marked": "^11.1.1",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
